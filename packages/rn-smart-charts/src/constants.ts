export const DEFAULT_LTTB_THRESHOLD = 2000;

export const DEFAULT_GRID = {
  left: 40,
  right: 12,
  top: 12,
  bottom: 24,
} as const;

export const DEFAULT_LINE_COLOR = '#3B82F6';
export const DEFAULT_LINE_WIDTH = 2.5;
export const DEFAULT_AREA_OPACITY = 0.35;

export const DEFAULT_AXIS_LABEL_COLOR = '#9AA0A6';
export const DEFAULT_AXIS_LABEL_FONT_SIZE = 11;

export const DEFAULT_ANIMATION_DURATION = 350;
export const Y_DOMAIN_SMOOTHING_DURATION = 200;
/** Threshold (fraction of current span) below which y-domain change is not re-animated. */
export const Y_DOMAIN_EPSILON = 0.005;

export const DEFAULT_LONG_PRESS_MS = 250;
export const DEFAULT_DECAY_DECELERATION = 0.998;

export const DEFAULT_X_TARGET_TICKS = 6;
export const DEFAULT_Y_TARGET_TICKS = 5;
